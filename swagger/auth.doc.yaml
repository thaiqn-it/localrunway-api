
components:
  securitySchemes:
   customerAuth:
    type: http
    scheme: bearer
    bearerFormat: JWT
   localbrandAuth:
    type: http
    scheme: bearer
    bearerFormat: JWT 
security:
  - customerAuth : []
  - localbrandAuth: []

